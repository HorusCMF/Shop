{% if tags is not empty %}

    <table class="table table-bordered table-striped">
        <thead>
        <tr>
            <th>#</th>
            <th>Mot-Clefs</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for tag in tags %}
            <tr>
                <td>{{ tag.id }}</td>
                <td><span class="label label-primary">{{ tag.word }}</span></td>
                <td>
                    <a onclick="return(confirm('Etes-vous sûr de vouloir supprimer ce tag ?'));"
                       href="{{ path('horus_site_remove_tag', {'id' : tag.id}) }}" class="btn btn-primary"><i
                                class="glyphicon glyphicon-remove"></i></a></h6>
                    <a href="{{ path('horus_site_edit_tag', {'id' : tag.id}) }}" class="btn btn-primary"><i
                                class="glyphicon glyphicon-pencil"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% else %}
    <div class="alert alert-dismissable alert-danger">
        <button type="button" class="close" data-dismiss="alert">×</button>
        <i class="glyphicon glyphicon-info-sign"></i> Aucun tag de créer pour le moment.
        <div class="clear"></div>
    </div>
{% endif %}